# Preview Environment Configuration
# Make Vercel environment variables available to the client
NEXT_PUBLIC_VERCEL_ENV=$VERCEL_ENV
NEXT_PUBLIC_VERCEL_GIT_COMMIT_REF=$VERCEL_GIT_COMMIT_REF

# Neon Database - This will be set in Vercel environment variables
# For preview environments, we'll use a branch based on the git branch name
DATABASE_URL=

# NextAuth.js - These will be set in Vercel environment variables
NEXTAUTH_SECRET=
# For preview deployments, we'll use the Vercel URL
NEXTAUTH_URL=

# Cloudinary Configuration
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your_cloud_name"
CLOUDINARY_API_KEY="your_api_key"
CLOUDINARY_API_SECRET="your_api_secret"
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET="your_upload_preset"
